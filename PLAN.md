# Plan: /begin-your-journey page

## Task
Add a course enrollment/onboarding page at `/begin-your-journey` for the Metaweb course by ISOC Nevada. Match the existing site aesthetic exactly.

## Approach
Single-file implementation: create `src/routes/begin-your-journey.tsx` (TanStack Router file-based routing auto-picks it up) and update `src/routes/index.tsx` header to add a nav link.

No new abstractions needed — both pages share Logo and footer pattern, but each page manages its own full layout (same pattern as existing index.tsx).

## Files to touch
- `src/routes/begin-your-journey.tsx` — new page (all 5 sections)
- `src/routes/index.tsx` — add Link import + "Enroll Now" nav button in header

## Sections for begin-your-journey.tsx
1. Hero — navy-deep bg, corner marks, headline, subheadline, electric date, video placeholder
2. What You Get — cream bg, dot-grid, 5-item feature grid (2 cols mobile / 3 desktop)
3. The Journey — 3 alternating cream/navy panels, one per week
4. Pricing — navy-deep centered, early bird badge, price, CTA
5. Footer — same pattern as index.tsx

## Risks / unknowns
- Route tree is auto-generated by TanStack Router Vite plugin — `begin-your-journey.tsx` filename in kebab-case is valid
- Build must be run to confirm no TS errors
